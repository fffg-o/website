# AES-256-GCM 加密密钥 (64 位十六进制 = 32 字节)
# 生成方法: openssl rand -hex 32
# 或: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CONTENT_KEY="0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
